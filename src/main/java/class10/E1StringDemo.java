package class10;

public class E1StringDemo {
    public static void main(String[] args) {
        // we have created an obj from the String class and have store  Cat inside it

        String obj=new String("Cat");//how we create an obj for String//we have to sdo this one
        //Shortcut to create objs from String class
        String obj2="My Dog";//this is also an obj like ^, only works with String//this is the shortcut
        int len=obj.length();//calling the length method/function/action/behaviour
        System.out.println(len);//its 3 b/c "cat" has 3 letters


    }
}
